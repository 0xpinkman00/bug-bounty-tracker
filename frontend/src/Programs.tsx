import {useState} from 'react';
import {ArrowLeft,ArrowUpRight,Box,ExternalLink,Search,ShieldCheck,Tag} from 'lucide-react';
import {Link,useParams, useSearchParams} from 'react-router-dom';
import {formatDate,sourceLabel,useApi,type Page,type Program} from './api';
import {Badge,Empty,ErrorBanner,EventRow,Loading,PageTitle,Pagination,Panel,TextLink} from './ui';
function platformTone(platform:string):'blue'|'purple'|'neutral'{return platform==='immunefi'?'blue':platform==='hackenproof'?'purple':'neutral'}
export function Programs(){const [params]=useSearchParams(),[platform,setPlatform]=useState(params.get('platform')||''),[search,setSearch]=useState(params.get('search')||''),[repository,setRepository]=useState(params.get('repository')||'all'),[offset,setOffset]=useState(0);const {data,error,loading}=useApi<Page<Program>>('/programs?offset='+offset+'&limit=25&search='+encodeURIComponent(search)+'&platform='+platform+'&repository='+repository);return <><PageTitle eyebrow="BOUNTY COVERAGE" title="Programs" description="Explore programs, scope, and the repositories behind them."/><div className="filter-bar"><div className="search-field"><Search size={18}/><input value={search} onChange={e=>{setSearch(e.target.value);setOffset(0)}} placeholder="Search programs..."/></div><select className="filter-select" value={platform} onChange={e=>{setPlatform(e.target.value);setOffset(0)}}><option value="">All platforms</option><option value="immunefi">Immunefi</option><option value="hackenproof">HackenProof</option><option value="manual">Manual</option></select><select className="filter-select" aria-label="Repository filter" value={repository} onChange={e=>{setRepository(e.target.value);setOffset(0)}}><option value="all">All programs</option><option value="linked">With Git repository</option><option value="unlinked">Without Git repository</option></select></div>{error&&<ErrorBanner message={error}/>}<Panel title="Tracked programs" subtitle={`${data?.total??0} programs in your workspace`} action={<TextLink to="/sources">Discover more</TextLink>} className="table-panel">{loading?<Loading/>:data?.items.length?<div className="data-table-wrap"><table className="data-table"><thead><tr><th>Program</th><th>Platform</th><th>Maximum bounty</th><th>Status</th><th>Latest release / update</th><th></th></tr></thead><tbody>{data.items.map(program=><tr key={program.id}><td><div className="table-primary"><span className="table-icon"><ShieldCheck size={18}/></span><div><Link className="table-title" to={'/programs/'+program.id}>{program.name}</Link><span className="table-subtitle">{program.platform_program_id||program.program_url}</span></div></div></td><td><Badge tone={platformTone(program.platform)}>{sourceLabel[program.platform]||program.platform}</Badge></td><td className="strong-cell">{program.max_bounty||'—'}</td><td><Badge tone={program.status==='active'?'green':'amber'}>{program.status}</Badge></td><td className="muted-cell">{formatDate(program.latest_activity_at,true)}</td><td><Link className="icon-button" to={'/programs/'+program.id} aria-label={'View '+program.name}><ArrowUpRight size={17}/></Link></td></tr>)}</tbody></table></div>:<Empty title="No programs found" description="Choose a source to discover bounty programs, or adjust your filters."/>}</Panel><Pagination total={data?.total??0} offset={offset} setOffset={setOffset}/></>}
export function ProgramDetail(){const {id}=useParams();const {data:program,error,loading}=useApi<Program>('/programs/'+id);if(loading)return <Loading/>;if(error)return <ErrorBanner message={error}/>;if(!program)return null;const inScope=program.assets?.filter(asset=>asset.in_scope)||[];const removed=program.assets?.filter(asset=>!asset.in_scope)||[];return <><Link className="back-link" to="/programs"><ArrowLeft size={16}/> Programs</Link><div className="detail-hero"><div className="detail-icon"><ShieldCheck size={28}/></div><div><div className="eyebrow">PROGRAM PROFILE</div><h1>{program.name}</h1><div className="detail-badges"><Badge tone={platformTone(program.platform)}>{sourceLabel[program.platform]||program.platform}</Badge><Badge tone={program.status==='active'?'green':'amber'}>{program.status}</Badge></div></div><a className="button button-secondary detail-visit" href={program.program_url} target="_blank" rel="noreferrer">Open bounty page <ExternalLink size={16}/></a></div><div className="detail-stats"><div><span>Maximum bounty</span><strong>{program.max_bounty||'Not specified'}</strong></div><div><span>In-scope assets</span><strong>{inScope.length}</strong></div><div><span>Repositories</span><strong>{program.repositories?.length||0}</strong></div><div><span>Last changed</span><strong>{formatDate(program.last_changed_at,true)}</strong></div></div><div className="detail-grid"><div className="detail-main"><Panel title="Current scope" subtitle="Assets eligible under this program"><div className="stack-list">{inScope.length?inScope.map(asset=><div className="stack-row" key={asset.id}><span className="stack-icon"><Box size={17}/></span><div><strong>{asset.type.replaceAll('_',' ')}</strong><span className="break-all">{asset.value}</span></div>{asset.url&&<a href={asset.url} target="_blank" rel="noreferrer" className="icon-button"><ArrowUpRight size={17}/></a>}</div>):<Empty title="No scope recorded" description="Scope appears after a source scrape."/>}</div>{removed.length>0&&<div className="scope-removed"><span>{removed.length} removed asset{removed.length===1?'':'s'} in history</span></div>}</Panel><Panel title="Recent program activity" subtitle="Program updates and scope changes">{program.events?.length?<div className="activity-feed">{program.events.slice(0,10).map(event=><EventRow key={event.id} event={event}/>)}</div>:<Empty title="No activity yet"/>}</Panel></div><div className="detail-side"><Panel title="Connected repositories" action={<TextLink to="/repositories">View all</TextLink>}>{program.repositories?.length?<div className="stack-list">{program.repositories.map(repo=><Link className="stack-row stack-link" to={'/repositories/'+repo.id} key={repo.id}><span className="stack-icon"><Box size={17}/></span><div><strong>{repo.owner}/{repo.name}</strong><span>{repo.default_branch||'Branch unknown'}</span></div><ArrowUpRight size={16}/></Link>)}</div>:<Empty title="None linked yet"/>}</Panel><Panel title="Recent releases">{program.releases?.length?<div className="stack-list">{program.releases.slice(0,6).map(release=><a className="stack-row stack-link" href={release.release_url} target="_blank" rel="noreferrer" key={release.id}><span className="stack-icon"><Tag size={17}/></span><div><strong>{release.tag}</strong><span>{formatDate(release.published_at,true)}</span></div><ArrowUpRight size={16}/></a>)}</div>:<Empty title="No releases yet"/>}</Panel></div></div></>}
